<Project>

  <PropertyGroup>
    <NuGetizeTweaksImported>true</NuGetizeTweaksImported>
    <NuspecFile>$(MSBuildThisFileDirectory)$(PackageId).nuspec</NuspecFile>
  </PropertyGroup>

  <Target Name="CopyFilesToOutputDirectory" />

</Project>